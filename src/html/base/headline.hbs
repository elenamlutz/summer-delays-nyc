<div class='newark_image'>
<div class="header">
    <p class="kicker">{{ kicker }}</p>
    <h1 class="headline">{{ headline }}</h1>
    <h2 class="subhead">JFK, Newark, LaGuardia and New York Stewart International handled more passengers in June 2019 than any other June on record.</h2>
    <p class="byline">
        {{#if author.url }}
            <a href="{{ author.url }}">{{ author.name }}</a>
        {{else}}
            {{ author.name }}
        {{/if}}
    </p>
    <p class="date">Published September 7, 2019</p>
</div>
</div>
